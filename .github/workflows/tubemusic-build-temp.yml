name: Temporary TubeMusic APK Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout TubeMusic branch
        uses: actions/checkout@v4
        with:
          ref: tubemusic-build
      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Android SDK
        uses: android-actions/setup-android@v3
      - name: Install SDK packages
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"
      - name: Gradle 9.6
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.0'
      - name: Rebuild and patch TubeMusic
        shell: bash
        run: |
          rm -rf TubeMusic TubeMusic_source.zip
          mkdir -p TubeMusic
          cat tubemusic_b64/part_* | base64 -d > TubeMusic_source.zip
          unzip -o TubeMusic_source.zip -d TubeMusic
          cat patches/MainActivity.part_* > TubeMusic/app/src/main/java/com/tubemusic/app/MainActivity.kt
          cp patches/ESoundPlaylist.kt TubeMusic/app/src/main/java/com/tubemusic/app/model/ESoundPlaylist.kt
          cp patches/ESoundPlaylistStore.kt TubeMusic/app/src/main/java/com/tubemusic/app/data/ESoundPlaylistStore.kt
          cp patches/JamendoRepository.kt TubeMusic/app/src/main/java/com/tubemusic/app/data/JamendoRepository.kt
          cp patches/app-build.gradle.kts TubeMusic/app/build.gradle.kts
      - name: Build debug APK
        working-directory: TubeMusic
        run: gradle --no-daemon :app:assembleDebug
      - name: Rename APK
        run: cp TubeMusic/app/build/outputs/apk/debug/app-debug.apk TubeMusic-debug.apk
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TubeMusic-debug-apk
          path: TubeMusic-debug.apk
          if-no-files-found: error
